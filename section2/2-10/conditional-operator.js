// if文
if ((year = 2020)) {
  alert("That's collect");
}

if (year < 2020) {
  alert("Too early...");
} else if (year > 2020) {
  alert("Too late...");
} else {
  alert("Exactly!");
}

// 3項演算子
// 条件式に括弧はなくても良い。
let courseName = price >= 10000 ? "高級コース" : "一般コース";
